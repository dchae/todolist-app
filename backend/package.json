{
  "name": "239-todos-backend-node",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend"
  },
  "dependencies": {
    "express": "~4.16.0",
    "sqlite3": "^4.0.2"
  }
}
